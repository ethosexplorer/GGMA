const fs = require('fs'); const content = fs.readFileSync('src/stateResources.ts', 'utf8'); const tempContent = fs.readFileSync('temp_stateResources.ts', 'utf8'); const azStart = tempContent.indexOf('"Arizona": {'); const caStart = tempContent.indexOf('"California": {'); const azArBlock = tempContent.substring(azStart, caStart); const currentCaStart = content.indexOf('"California": {'); fs.writeFileSync('src/stateResources.ts', content.substring(0, currentCaStart) + azArBlock + content.substring(currentCaStart)); console.log('Fixed');
